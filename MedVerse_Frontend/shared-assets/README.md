# Shared Assets

Put images and other static files used by more than one app in this folder.

All apps serve this folder at:

```text
/shared-assets/<file-name>
```

Example:

```html
<img src="/shared-assets/home-hero.png" alt="MedAuth security" />
```
